{
"scene": {
"login": {
"success": "登录成功",
"failed": "登录失败"
}
}
}

@Component
public class MessageConfig {

    @NacosConfig(
            dataId = "msg_scene.json",
            value = "scene.login.success"
    )
    private String loginSuccess;

    @NacosConfig(
            dataId = "msg_scene.json",
            value = "scene.login.failed"
    )
    private String loginFailed;
}