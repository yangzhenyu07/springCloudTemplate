<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.example</groupId>
  <artifactId>yzy-dependencies-bom</artifactId>
  <packaging>pom</packaging>
  <version>1.0-SNAPSHOT</version>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <java.version>1.8</java.version>

    <!-- ====== 版本集中管理（修改版本只需改这里） ====== -->
    <spring-boot.version>2.6.15</spring-boot.version>
    <spring-cloud.version>2021.0.9</spring-cloud.version>
    <spring-cloud-alibaba.version>2021.0.6.0</spring-cloud-alibaba.version>
    <hutool.version>5.8.15</hutool.version>
    <knife4j.version>3.0.3</knife4j.version>
    <springfox.version>3.0.0</springfox.version>
    <mybatis-plus.version>3.5.2</mybatis-plus.version>
    <fastjson2.version>2.0.47</fastjson2.version>
    <!-- logstash-logback-encoder: 7.3 是最后支持 logback 1.2.x（Boot 2.6）的版本；7.4+ 需要 logback 1.3+ -->
    <logstash-logback-encoder.version>7.3</logstash-logback-encoder.version>
    <!-- RocketMQ 官方 Spring Boot Starter：2.2.2 对应 rocketmq-client 4.9.3，兼容 Boot 2.x / Java 8。
         （bes-cloudmq-h-clernt-all 为宝兰德私有依赖，本地仓库不可得，经确认改用官方 starter） -->
    <rocketmq-spring-boot-starter.version>2.2.2</rocketmq-spring-boot-starter.version>
    <internal.version>1.0.1</internal.version>
  </properties>
  <name>yzy-dependencies-bom</name>

  <dependencyManagement>
    <dependencies>
      <!-- Spring Boot BOM -->
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Spring Cloud BOM -->
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <!-- Spring Cloud Alibaba BOM (Nacos 2.2.0) -->
      <dependency>
        <groupId>com.alibaba.cloud</groupId>
        <artifactId>spring-cloud-alibaba-dependencies</artifactId>
        <version>${spring-cloud-alibaba.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Internal modules -->
      <dependency>
        <groupId>org.example.redis</groupId>
        <artifactId>redis-module</artifactId>
        <version>${internal.version}</version>
      </dependency>
      <dependency>
        <groupId>com.example.common</groupId>
        <artifactId>common-module-v1</artifactId>
        <version>${internal.version}</version>
      </dependency>
      <dependency>
        <groupId>com.example.common</groupId>
        <artifactId>common-module-v2</artifactId>
        <version>${internal.version}</version>
      </dependency>
      <dependency>
        <groupId>com.example.common</groupId>
        <artifactId>springCloud-module</artifactId>
        <version>${internal.version}</version>
      </dependency>
      <dependency>
        <groupId>com.example</groupId>
        <artifactId>yzy-gateway</artifactId>
        <version>1.0-SNAPSHOT</version>
      </dependency>
      <dependency>
        <groupId>com.example</groupId>
        <artifactId>yzy-b-demo</artifactId>
        <version>1.0-SNAPSHOT</version>
      </dependency>

      <!-- Third-party (versions not managed by Spring Boot or imported BOMs) -->
      <dependency>
        <groupId>cn.hutool</groupId>
        <artifactId>hutool-all</artifactId>
        <version>${hutool.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.xiaoymin</groupId>
        <artifactId>knife4j-spring-boot-starter</artifactId>
        <version>${knife4j.version}</version>
      </dependency>
      <dependency>
        <groupId>io.springfox</groupId>
        <artifactId>springfox-boot-starter</artifactId>
        <version>${springfox.version}</version>
      </dependency>
      <dependency>
        <groupId>com.baomidou</groupId>
        <artifactId>mybatis-plus-boot-starter</artifactId>
        <version>${mybatis-plus.version}</version>
      </dependency>
      <dependency>
        <groupId>com.alibaba.fastjson2</groupId>
        <artifactId>fastjson2</artifactId>
        <version>${fastjson2.version}</version>
      </dependency>
      <!-- JSON 日志输出（ELK/filebeat 采集用） -->
      <dependency>
        <groupId>net.logstash.logback</groupId>
        <artifactId>logstash-logback-encoder</artifactId>
        <version>${logstash-logback-encoder.version}</version>
      </dependency>
      <!-- RocketMQ Spring Boot Starter（生产者/消费者统一版本入口） -->
      <dependency>
        <groupId>org.apache.rocketmq</groupId>
        <artifactId>rocketmq-spring-boot-starter</artifactId>
        <version>${rocketmq-spring-boot-starter.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>
